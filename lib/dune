(library
 (name minicc)
 (libraries core llvm llvm.analysis)
 (preprocess
  (pps ppx_deriving.show)))

(rule
  (targets parser.ml)
    (deps parser.mly)
    (action (run menhir parser.mly)))

(ocamllex lexer)
